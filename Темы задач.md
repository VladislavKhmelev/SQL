
~~~
SELECT
WHERE
GROUP BY
HAVING
COUNT()
AVG()
ORDER BY
LIMIT
INNER JOIN
LEFT JOIN
ON — условие соединения таблиц
разницу между WHERE и HAVING
разницу между условием в ON и WHERE при LEFT JOIN
PRIMARY KEY / FOREIGN KEY — базовое понимание связей таблиц.
~~~
