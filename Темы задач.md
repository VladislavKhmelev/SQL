
~~~
SELECT
WHERE
ORDER BY
LIMIT
GROUP BY
HAVING
PRIMARY KEY
FOREIGN KEY
INNER JOIN
LEFT JOIN



WHERE + GROUP BY
GROUP BY + HAVING
COUNT() + AVG()
ORDER BY + LIMIT
INNER JOIN + фильтрация
LEFT JOIN + COUNT()


•	Subquery 
•	коррелированный Subquery 
•	WITH / CTE 
•	несколько CTE 
•	GROUP BY 
•	HAVING 
•	JOIN 
•	ON 
•	WHERE 
•	AVG 
•	MAX 
•	COUNT 
•	алиасы

ROW_NUMBER()
RANK()
DENSE_RANK()
LAG()
LEAD()
SUM() OVER()
AVG() OVER()
ROWS BETWEEN

разницу ON и WHERE при LEFT JOIN
связь PRIMARY KEY ↔ FOREIGN KEY
создание таблиц через CREATE TABLE
INSERT INTO + VALUES
SERIAL, INTEGER, NUMERIC
~~~
